#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() 
{
    int array_size;
    cin >> array_size;
    
    int Array[array_size];
    
    for (int i = 0; i< array_size; i++) 
    {
        cin >> Array[i];
    }
    
    for (int i = array_size - 1; i > -1; i--)
    {
        cout << Array[i] << " ";
    } 
    return 0;
}
